'use strict';

angular.module('fullstackApp')
  .controller('MainCtrl', ['$scope', '$location', 
    function($scope, $location){
        // $scope.goto = function(eID){
        //     $location.hash(eID);
        //     anchorSmoothScroll.scroll();
        // };
    }
  ]);
